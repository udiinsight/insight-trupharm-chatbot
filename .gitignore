# OS
.DS_Store
Thumbs.db

# Editors / tooling
.vscode/
.idea/
*.swp
*.swo
.claude/

# Node / build
node_modules/
*.log
npm-debug.log*

# PHP
vendor/
composer.lock

# WordPress
*.zip
wp-content/

# Local dev
.env
.env.local
*.local

# Never commit secrets (public repo)
keys.txt
*-keys.json
insight-keys.json

# Lint outputs
.phpunit.result.cache
.phpcs-cache

# Test / browser artifacts
.playwright-mcp/
/*.png

# Client source documents (raw originals — keep out of the public repo)
Knowlage source docs/

# Internal session handoff (operational detail — local only)
HANDOFF.md

# Local extraction scratch (doc → text, not for the repo)
.tmp-extract/
